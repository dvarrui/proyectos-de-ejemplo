from setuptools import setup, find_packages

setup(
    # El nombre debe coincidir con el nombre de la carpeta
    name='helloworld',
    version='0.0.1',
    author='dvarrui',
    author_email="dvarrui@gmail.com",
    description="Demo helloworld package",
    packages=find_packages(),
    # install_requires=[],
)
